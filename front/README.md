
  # Local Business Finder

  This is a code bundle for Local Business Finder. The original project is available at https://www.figma.com/design/S5PT7XAB8Evd7lagVAOx2V/Local-Business-Finder.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  